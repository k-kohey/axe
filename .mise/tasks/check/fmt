#!/usr/bin/env bash
#MISE description="Format Go and Swift code, then lint Swift"

set -euo pipefail
gofmt -w cmd
xcrun swift-format format --in-place --recursive "$SWIFT_ANALYSIS/Sources" "$SWIFT_ANALYSIS/Tests"
xcrun swift-format lint --recursive "$SWIFT_ANALYSIS/Sources" "$SWIFT_ANALYSIS/Tests"
